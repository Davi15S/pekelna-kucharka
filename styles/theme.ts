export type Theme = {
  colors: {
    primary: string;
    secondary: string;
  };
  background: {
    primary: string;
  };
};

export const theme = {
  colors: {
    primary: "#FFFFFF",
    secondary: "#B7B7B7",
  },
  background: {
    primary: "#111111",
  },
};
